<!DOCTYPE html>
<html>
  <head>
    <title>LovelyBaby</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/9f7238856e.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=M+PLUS+Rounded+1c:500&display=swap" rel="stylesheet">
  </head>

  <body>
    <header class="header">
      <div class="container">
          <nav>
            <ul class="nav navbar-right">
        <% if customer_signed_in?
               if request.fullpath.index("/admin/") == nil%>
               <span><%= link_to "相談", problems_path  %></span>
               <span><%= link_to "ログアウト", destroy_customer_session_path, method: :delete %></span>
               <span><%= link_to "当サイトについて",homes_about_path  %></span>
            </ul>
            <nav class="baby_name">
              <% if @baby && @baby.baby_name %>
                <%= link_to @baby.baby_name, babies_path %>
              <% end %>
             </nav>
        <% end %>
          <nav class="nav navbar-right">
            <% elsif admin_signed_in? %>
                　<%= link_to "ログアウト", destroy_admin_session_path, method: :delete %>
            <% else %>
                    <%= link_to "当サイトについて",homes_about_path  %>
                    <%= link_to "ログイン", new_customer_session_path %>
                    <%= link_to "管理者の方はこちら", new_admin_session_path %>
            <% end %>
          </nav>
      </nav>
      </div>
    </header>
    <%= yield %>
       <footer class="footer">
      <div class="container">
        <div class="row">
          <div class="col-xs-3 col-xs-offset-9">
          <p>Lovely Baby</p>
        </div>
        </div>
      </div>
    </footer>
  </body>
</html>
