<div class="container">
	<div class="admin-box">
		<h3>子育て相談室</h3>
		<h4 style="color:red;">※不適切な投稿を削除してください</h4>
		<table class="table">
			<tbody>
				<% @problems.each do |problem| %>
					<tr>
						<td><%= link_to problem.body,admins_problem_path(problem) %></td>
						<td><%= link_to admins_problem_path(problem), method: :delete do %>
						<i class="far fa-trash-alt"></i></td>
						<% end %>
			<!-- 		<% if problem.score.to_i <=  -0.4  %>
						<td class="score_comment2"><i class="fas fa-exclamation-triangle"></i>投稿内容に応じて削除して下さい</td>
					<% end %> -->
					</tr>
				<% end %>
			</tbody>
		</table>
	</div>
</div>